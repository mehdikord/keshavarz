/** Default map center (Gorgan) used when no saved location exists. */
export const DEFAULT_MAP_CENTER = {
  lat: 36.842138,
  lng: 54.441192,
} as const;

/** Default "city overview" zoom used when no saved location exists. */
export const DEFAULT_MAP_OVERVIEW_ZOOM = 11;
